apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-testing.fullname" . }}
  labels:
    {{- include "llm-testing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llm-testing.selectorLabels" . | nindent 4 }}
